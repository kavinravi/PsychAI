# ---------- core HF stack ----------
transformers>=4.55.1
peft>=0.12.0
trl>=0.9.6
accelerate>=0.33.0
datasets>=2.19.0
evaluate>=0.4.1
safetensors>=0.4.3
huggingface_hub>=0.23.0
bitsandbytes>=0.42.0   # int8 path for LoRA
google-generativeai>=0.7.0


# ---------- essentials / utilities ----------
numpy>=1.26
pandas>=2.2
tqdm>=4.66
sentencepiece>=0.2.0   # some tokenizers still need it
python-dotenv>=1.0.1
tqdm>=4.66.3


# ---------- notebooks & collaboration ----------
ipykernel>=6.29
jupytext>=1.16
nbdime>=4.0.2


# ---------- web application (Streamlit) ----------
streamlit>=1.31.0
streamlit-authenticator>=0.3.1  # For custom authentication
google-auth>=2.27.0  # For Google OAuth
google-auth-oauthlib>=1.2.0  # For Google OAuth flow
google-auth-httplib2>=0.2.0  # For Google OAuth

# ---------- database ----------
supabase>=2.3.0  # PostgreSQL database client
psycopg2-binary>=2.9.9  # PostgreSQL adapter (backup/direct access)


# have to install torch separately due to CUDA version
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
